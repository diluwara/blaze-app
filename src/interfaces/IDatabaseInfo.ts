export interface IDatabaseInfo {
  id: number;
  instance_name: string;
  folder: string;
  ip_address: string;
  port: number;
  pid: number;
  max_memory: string;
  min_memory: string;
  status: string;
}
